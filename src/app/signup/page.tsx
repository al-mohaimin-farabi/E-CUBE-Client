import { redirect } from 'next/navigation';

// Signup page - redirect to home for now
// This page will be used for user account creation later
const SignupPage = () => {
  return (
    <main>
      <h1>Signup Page</h1>
    </main>
  );
};

export default SignupPage;
